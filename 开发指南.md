# Aki Kirari 前端开发指南

## 1. 项目定位

`akisaki-kirari` 是虚拟桌宠项目的桌面前端，使用 `Electron + Vue 3 + Vite + TypeScript` 实现。它既不是传统浏览器网页，也不只是一个普通聊天窗口，而是一个包含以下能力的桌面应用：

- 悬浮桌宠窗口
- 系统托盘交互
- 独立聊天窗口
- 与后端 WebSocket 服务实时通信
- 本地持久化聊天记录

可以把它理解成“两层架构”的桌面客户端：

1. Electron 主进程负责窗口、托盘、IPC、会话服务生命周期。
2. Vue 渲染进程负责桌宠 UI、聊天 UI 和用户交互体验。

## 2. 技术栈概览

### 核心技术

- 桌面容器：Electron
- 前端框架：Vue 3
- 构建工具：Vite
- 语言：TypeScript
- Electron 集成插件：`vite-plugin-electron`

### 当前依赖特点

- 没有引入状态管理库，如 Pinia
- 没有引入 UI 组件库
- 依赖比较轻，代码结构偏手写和直连

这种结构很适合原型开发：易读、可控、启动快，但随着功能增长，需要逐步加强模块边界。

## 3. 目录结构说明

```text
akisaki-kirari/
├─ electron/
│  ├─ main.ts                 # 主进程：窗口/托盘/IPC/签名/Token 调用
│  ├─ chat-session-service.ts # WS 中枢 + 重连 + 持久化
│  └─ preload.ts              # 仅设置窗使用的安全桥（contextBridge）
├─ public/
│  ├─ favicon.svg
│  ├─ icons.svg
│  └─ tray.png
├─ src/
│  ├─ assets/
│  ├─ components/
│  │  ├─ chat/
│  │  │  └─ ChatWindow.vue
│  │  ├─ pet/
│  │  │  ├─ PetAvatar.vue
│  │  │  ├─ PetBubble.vue
│  │  │  ├─ PetStage.vue
│  │  │  └─ PetToolbar.vue
│  │  └─ settings/
│  │     └─ ApiTokenSettings.vue   # 设置窗：配置 API Token / 模型 / 端点
│  ├─ composables/
│  │  ├─ useChatSocket.ts          # 桥接 IPC 与 Pinia chat store
│  │  └─ useApiToken.ts            # 封装 window.tokenApi 调后端 Token 接口
│  ├─ stores/
│  │  ├─ chat.ts                   # 聊天/未读状态（含未读计数与对话框打开标记）
│  │  └─ pet.ts                    # 桌宠可见性/提示语等本地状态
│  ├─ App.vue                      # 按 ?window= 切换 桌宠/聊天/设置
│  ├─ main.ts
│  └─ style.css
├─ index.html
├─ vite.config.ts
├─ tsconfig.json
└─ package.json
```

> 说明：本文档早期版本漏写了 `electron/preload.ts`、`src/components/settings/ApiTokenSettings.vue`、`src/composables/useApiToken.ts`，且未描述"设置窗口"。这三处**已经实装**。

## 4. 整体架构理解

### 架构分层

#### Electron 主进程

主进程核心文件是 `electron/main.ts`，负责：

- 创建桌宠窗口
- 创建聊天窗口
- 创建系统托盘和菜单
- 处理窗口显示、隐藏、重置位置
- 处理来自渲染层的 IPC 事件
- 初始化 `ChatSessionService`

#### 会话服务层

`electron/chat-session-service.ts` 负责：

- 连接后端 WebSocket
- 持有前端侧聊天状态
- 将聊天状态广播给所有窗口
- 将聊天记录持久化到本地磁盘
- 在断线后自动重连

这一层非常关键，它相当于“Electron 应用内的轻量状态中心”。

#### Vue 渲染层

`src/` 目录负责具体界面：

- `App.vue` 根据 URL 参数判断当前渲染桌宠窗口还是聊天窗口
- `PetStage.vue` 负责桌宠主体交互
- `ChatWindow.vue` 负责聊天面板
- `useChatSocket.ts` 负责把主进程 IPC（`chat:state` / `chat:open-changed`）桥接到 Pinia chat store
- `stores/pet.ts`（Pinia）负责桌宠可见性与提示语等本地状态
- `stores/chat.ts`（Pinia）集中聊天消息、连接状态与未读计数；**注意**：桌宠窗口与对话框是两个独立渲染进程，Pinia 仅在进程内共享，跨进程的“对话框打开/关闭”由主进程经 `chat:open-changed` 推入 store

## 5. 启动方式与开发模式

### 当前脚本

`package.json` 中存在这些脚本：

```json
{
  "dev": "vite",
  "build": "vite build",
  "preview": "vite preview",
  "electron:dev": "vite"
}
```

### 实际开发体验

由于项目启用了 `vite-plugin-electron`，执行：

```bash
npm run dev
```

通常会同时拉起：

- Vite 开发服务器
- Electron 主进程

也就是说，当前的 `dev` 既承担前端开发服务器职责，也承担桌面应用开发入口职责。

### 构建

```bash
npm run build
```

这个命令会完成 Vite 构建与 Electron 相关产物构建，但它不等价于“打包安装包”。如果后续需要生成可分发的 Windows 安装包，还需要接入如 `electron-builder` 或 `electron-forge`。

## 6. 主窗口与聊天窗口机制

项目不是单窗口，而是两个窗口协同：

### 1. 桌宠窗口

在 `electron/main.ts` 的 `createWindow()` 中创建，特点包括：

- 无边框
- 透明背景
- 永远置顶
- 不显示在任务栏
- 初始位置靠近屏幕右下角

这让它看起来更像一个真正的桌面宠物，而不是一个普通应用窗口。

### 2. 聊天窗口

在 `createChatWindow()` 中创建，特点包括：

- 独立窗口
- 常规矩形应用界面
- 可最小化、可聚焦
- 通过 `?window=chat` 区分渲染内容

### 3. 窗口切换策略

- 托盘菜单可以显示/隐藏桌宠
- 点击相关菜单项可以打开聊天框
- 桌宠气泡在特定情况下可直接引导打开聊天窗口

### 4. 设置窗口

托盘菜单的"打开设置"会创建独立的设置窗口（`createSettingsWindow()`），用于配置模型名 / 模型端点 / API Token。它渲染 `src/components/settings/ApiTokenSettings.vue`（通过 `?window=settings` 区分）。

特点：

- 独立窗口，常规矩形界面
- **安全配置**：使用 `preload.js` + `contextIsolation: true` + `nodeIntegration: false`
- 通过 `window.tokenApi` 安全访问主进程的接口

### 5. 配置驱动的工作模式（deploy config）

前端启动时不写死后端地址，而是读取"安装过程"生成的配置文件 `pet-client.config.json`（查找顺序：`PET_CLIENT_CONFIG` 环境变量 → `userData/pet-client.config.json`；缺省回退为本地模式 localhost:9089）。配置决定两种工作模式：

```json
{ "mode": "local", "server": { "wsUrl": "ws://localhost:9089/ws", "httpUrl": "http://localhost:9089" }, "builtinToken": "kirari-local-builtin" }
```

- **local（本地部署）**：未选定服务端地址。前端使用客户端与服务端共同拥有的**内置账户**（`uid=0`，共享 `builtinToken`），设置界面打开即直接可用，无需登录、无需查看密码。
- **remote（远程服务端）**：安装时选定了服务端地址。设置界面顶部出现**登录板块**，输入用户名/密码与服务端验证通过后，下方配置功能才启用；退出登录即锁定。

### 6. 设置窗口的账户与配置体系（无感）

设置窗不直接持有后端密钥，而是经由主进程代理，按**账户凭证**驱动：

1. 主进程启动时 `loadClientConfig()` 读取 `pet-client.config.json`，据此派生后端 WS / HTTP 地址与模式。
2. 请求走 `requestSettingsApi()`：
    - local 模式附带 `X-Builtin-Token: <builtinToken>`（对应后端 `BUILTIN_ACCOUNT_TOKEN`，内置账户 `uid=1`）；
    - remote 模式附带登录后拿到的 `Authorization: Bearer <sessionToken>`。
3. 登录由主进程 `token:login` IPC 调后端 `POST /api/auth/login` 完成，会话令牌保存在主进程内存。
4. 配置读写用 `GET/PUT /api/profile`：Token 以掩码返回（如 `sk-****abcd`），改动只需填入新值直接替换——**不再需要"查看密码"**。
5. 聊天 WebSocket 复用同一身份：主进程 `ensureChatSessionService()` 按模式把 `builtinToken`（local）或登录后的 `sessionToken`（remote）拼到 WS 握手 URL 的 `?token=`，后端于握手阶段校验；未带/无效令牌直接拒绝连接，且连接身份强制为令牌对应的 `uid`（忽略客户端自报 `userid`）。

> 旧的 ed25519 客户端签名链路（`client-identity.json` / `signTokenRequest`）已不再用于设置界面，仅后端 legacy `/api-token/*` 接口保留。

> uid 规则：后端 `0-99999` 为程序保留账户（内置账户 = 0）；独立部署服务端注册的真实用户从 `100000` 起。

## 7. URL 路由约定

项目没有使用 `vue-router`，而是采用一种极简区分方式：

- 默认窗口：渲染 `PetStage`
- 带 `?window=chat`：渲染 `ChatWindow`

`App.vue` 中通过：

```ts
const windowType = new URLSearchParams(window.location.search).get('window')
const isChatWindow = windowType === 'chat'
```

来决定展示哪个界面。

### 优点

- 简单直接
- 不需要引入路由依赖
- 很适合 Electron 多窗口场景

### 局限

- 页面类型一多就会变得难管理
- 渲染入口耦合到查询参数

如果未来增加设置页、记忆页、日志页，可以考虑：

- 为不同窗口建立独立入口
- 或引入更清晰的路由与窗口装配方式

## 8. 桌宠交互链路

桌宠主体在 `src/components/pet/PetStage.vue`。

### 当前交互能力

- 鼠标拖拽移动桌宠
- 右键弹出上下文菜单
- 点击可交互气泡打开聊天窗口
- 根据对话状态更新气泡内容

### 拖拽流程

拖拽并不是在渲染层里直接修改窗口坐标，而是：

1. Vue 捕获 `pointerdown`
2. 通过 IPC 发送 `desktop-pet:drag-start`
3. 主进程记录窗口初始位置和鼠标起始位置
4. 渲染层持续发送 `desktop-pet:drag-move`
5. 主进程根据鼠标屏幕坐标实时更新窗口位置
6. 结束拖拽后发送 `desktop-pet:drag-end`

这种做法是正确的，因为真正能安全操作原生窗口位置的是主进程。

### 气泡显示逻辑

桌宠气泡来自两套状态：

- 默认本地消息，由 `stores/pet.ts`（Pinia）提供
- 来自聊天服务的动态消息，由 `useChatSocket.ts` 桥接到 `stores/chat.ts` 提供

气泡是否显示现在由**未读消息数**驱动：主进程在对话框打开时发送 `chat:open-changed true`（清零未读），关闭时发送 `false`；期间收到 pet 新消息才重新冒泡，不再因焦点变化重复弹出。

`PetStage.vue` 会根据拖拽状态、气泡是否被关闭、消息长度等条件决定：

- 展示什么文本
- 是否可点击
- 是否要隐藏

## 9. 聊天链路

### 关键文件

- 渲染层入口：`src/components/chat/ChatWindow.vue`
- 渲染层适配：`src/composables/useChatSocket.ts`
- 主进程服务：`electron/chat-session-service.ts`

### 数据流方向

整个聊天不是 Vue 直接连接后端，而是先走 Electron 主进程：

1. `ChatWindow.vue` 收集用户输入
2. `useChatSocket.ts` 调用 `ipcRenderer.send("chat:send-message", text)`
3. `electron/main.ts` 收到 IPC 事件
4. `ChatSessionService.sendMessage()` 把消息发给后端 WebSocket
5. 后端返回 `pet_response`
6. `ChatSessionService` 更新内部状态
7. 主进程通过 `chat:state` 广播给所有窗口
8. 桌宠窗口和聊天窗口一起刷新

### 这种设计的优点

- WebSocket 连接只维护一份
- 多窗口共享同一份聊天状态
- 持久化逻辑集中在主进程，渲染层更轻

### 需要注意的点

- 这种设计让渲染层依赖 Electron IPC，无法直接当纯网页运行
- 如果后续要做 Web 版，需要再抽一层通信适配器

## 10. `ChatSessionService` 的作用

这个类是当前前端最核心的状态与通信中枢。

### 主要职责

- 生成并维护 `sessionId`
- 管理 `connected` 状态
- 管理消息数组 `messages`
- 管理桌宠当前气泡文案 `bubbleMessage`
- 管理气泡是否可点击 `bubbleInteractive`
- 处理断线重连
- 将状态写入本地 `chat-session.json`

### 状态持久化

应用启动时，主进程调用：

```ts
ensureChatSessionService().init(path.join(app.getPath("userData"), "chat-session.json"))
```

说明聊天记录会保存到 Electron 用户数据目录下，而不是项目源码目录。

这意味着：

- 重启应用后，聊天记录仍可恢复
- 不会污染 Git 工作区
- 真机环境更接近实际桌面应用行为

### 断线处理

当前实现包含自动重连机制：

- 连接异常后设置 `lastError`
- 标记 `connected = false`
- 3 秒后自动尝试重连

这对桌面宠物类应用非常重要，因为它不应该因为后端临时重启就完全失效。

## 11. 与后端的联调约束

这是当前最需要开发者注意的一节。

### 当前后端地址通过环境变量配置

在 `electron/main.ts` 中，后端地址优先从环境变量读取：

```ts
const backendUrl = process.env.VITE_BACKEND_WS_URL ?? "ws://localhost:9089/ws";
const backendHealthUrl = process.env.VITE_BACKEND_HEALTH_URL ?? "http://localhost:9089/health";
```

这意味着前端默认会连接本地 9089 端口，但也可以通过 `.env` 自定义。

### 推荐做法

开发期间建议在本地 `.env` 中保留下面的配置：

- `VITE_BACKEND_WS_URL=ws://localhost:9089/ws`
- `VITE_BACKEND_HEALTH_URL=http://localhost:9089/health`

仓库中已经提供了 `.env.example` 作为模板，实际 `.env` 保持本地文件即可，不要提交到远端。

### 联调风险

后端 `pet-api` 当前有端口自动探测逻辑，如果 `9089` 被占用，它可能退到其他端口，但前端不会自动发现这个变化。

### 更合理的后续方案

建议未来把后端地址继续抽象成可视化设置项，例如：

- 从环境变量读取
- 从本地设置文件读取
- 启动时探测 `/health`

## 12. IPC 通信约定

当前渲染层和主进程通过 IPC 通信，主要通道包括：

### 渲染层发送到主进程

- `chat:send-message`
- `desktop-pet:drag-start`
- `desktop-pet:drag-move`
- `desktop-pet:drag-end`
- `desktop-pet:open-chat`
- `desktop-pet:show-context-menu`
- `desktop-pet:reset-position`

### 渲染层调用主进程

- `chat:get-state`

### 主进程广播给渲染层

- `chat:state`

### 开发建议

如果后续 IPC 类型继续增加，建议提取成独立常量文件或类型定义文件，避免：

- 字符串散落
- 通道名拼写错误
- 渲染层和主进程定义不一致

## 13. 组件职责说明

### `ChatWindow.vue`

- 展示对话历史
- 展示连接状态
- 接收用户输入
- 发送消息
- 自动滚动到底部

### `PetStage.vue`

- 聚合桌宠头像和气泡
- 负责拖拽交互
- 负责右键菜单唤起
- 控制气泡是否可点击、是否隐藏

### `PetAvatar.vue`

- 负责渲染桌宠形象图
- 当前主要是纯展示组件

### `PetBubble.vue`

- 负责渲染桌宠气泡
- 根据 `interactive` 决定点击行为

### `PetToolbar.vue`

- 当前项目中并未实际接入主界面
- 更像是一个预留控制条组件

如果后续确定不用，可以删除；如果打算用于调试模式，可以考虑只在开发环境挂载。

## 14. 样式系统说明

项目样式集中在 `src/style.css`，特点如下：

- 使用 CSS 变量统一主题色
- 桌宠窗口与聊天窗口共用一个样式文件
- 样式比较扁平，没有采用 CSS Modules 或原子化方案

### 当前优点

- 简单直接
- 修改方便
- 小项目维护成本低

### 当前风险

- 文件会随着组件增多变得越来越长
- 样式作用域不够清晰
- 多窗口样式容易互相影响

### 后续建议

可以逐步过渡到：

- 组件级局部样式
- 或按模块拆分 CSS 文件

## 15. Electron 安全注意事项

各窗口的安全配置**并不一致**，需特别注意：

| 窗口     | nodeIntegration | contextIsolation | preload | 状态     |
| -------- | --------------- | ---------------- | ------- | -------- |
| 桌宠窗   | `true`          | `false`          | 无      | ⚠️ 高危 |
| 聊天窗   | `true`          | `false`          | 无      | ⚠️ 高危 |
| 设置窗   | `false`         | `true`           | 有      | ✅ 安全 |

桌宠窗与聊天窗仍使用：

- `nodeIntegration: true`
- `contextIsolation: false`

这对原型开发方便（渲染层可直接 `window.require("electron")`），但在正式产品里属于高风险配置：渲染进程权限过大，更容易受注入类风险影响。

**设置窗已经实装了安全方案**：使用 `electron/preload.ts` + `contextIsolation: true` + `nodeIntegration: false`，仅通过 `contextBridge` 暴露 `window.tokenApi`（供 `useApiToken.ts` 调用）。

### 原因（为何桌宠/聊天窗高危）

- 渲染进程权限过大
- 更容易受到注入类风险影响

### 正式化建议

后续建议把桌宠窗与聊天窗也迁移为：

- `contextIsolation: true`
- `nodeIntegration: false`
- 通过 `preload` 脚本显式暴露安全 API（如把 `useChatSocket.ts` 当前直接用的 `window.require("electron").ipcRenderer` 改为经 preload 暴露）

这会让工程稍复杂，但更符合 Electron 最佳实践，且与设置窗保持一致。

## 16. 本地开发建议流程

### 第一步：安装依赖

```bash
npm install
```

### 第二步：先启动后端

确保后端 `pet-api` 已在 `9089` 端口启动（与前端默认 `VITE_BACKEND_WS_URL=ws://localhost:9089/ws` 一致）。

> 已知小问题：主进程 `checkBackendHealth()` 在后端未启动时打印的提示文案仍写"端口 8089"，属历史笔误，不影响功能；实际默认端口为 `9089`。

### 第三步：启动桌宠前端

```bash
npm run dev
```

### 第四步：验证关键功能

至少确认下面这些点：

- 桌宠窗口成功显示
- 托盘图标成功出现
- 点击菜单可以打开聊天框
- 聊天框状态显示为在线
- 输入消息后可以收到后端回复
- 关闭应用再打开后，聊天记录仍能恢复

## 17. 常见问题排查

### 1. 聊天框一直显示离线

优先检查：

- 后端是否已启动
- 后端是否真的监听在 `9089`（注意：后端启用了端口自动探测，若 9089 被占用会漂移，前端不会自动发现）
- WebSocket 路径是否为 `/ws`

### 2. 桌宠窗口不显示

优先检查：

- Electron 是否成功启动
- 主进程是否执行到了 `createWindow()`
- 是否被托盘逻辑隐藏了

### 3. 消息能发但没有回复

优先检查：

- 后端 WebSocket 是否收到消息
- 后端消息格式是否正确
- `ChatSessionService.handleMessage()` 是否成功解析返回内容

### 4. 聊天记录没有保存

优先检查：

- Electron 用户数据目录是否可写
- `chat-session.json` 是否被正常创建
- 持久化写入是否在控制台报错

## 18. 后续演进建议

### 近期建议

- 把 `backendUrl` 做成配置项
- 增加更明确的开发/生产环境区分
- 整理 IPC 通道常量
- 为 `ChatSessionService` 增加更稳定的类型约束

### 中期建议

- 引入 `preload` 方案，替换当前不安全的 Electron 配置
- 抽离通信层，支持 Web 端和 Electron 端共用聊天逻辑
- 将窗口逻辑按模块拆分，降低 `electron/main.ts` 复杂度

### 长期建议

- 增加桌宠动作系统
- 增加设置面板
- 增加可配置角色、皮肤、语音
- 增加本地记忆与用户偏好同步能力

## 19. 新人接手时建议先读哪些文件

建议按下面顺序阅读：

1. `package.json`
2. `vite.config.ts`
3. `electron/main.ts`
4. `electron/chat-session-service.ts`
5. `src/App.vue`
6. `src/composables/useChatSocket.ts`
7. `src/components/pet/PetStage.vue`
8. `src/components/chat/ChatWindow.vue`
9. `electron/preload.ts`（设置窗安全桥：getDeployConfig / login / request）
10. `src/composables/useApiToken.ts` + `src/components/settings/ApiTokenSettings.vue`（按账户凭证无感配置）
11. `electron/main.ts` 中的 `loadClientConfig` / `requestSettingsApi` / `token:login`（配置驱动 + 会话鉴权）

这样可以最快建立这条主线：

- 应用怎么启动
- Electron 怎么创建窗口
- WebSocket 怎么被主进程托管
- Vue 怎么通过 IPC 取状态和发消息
- 桌宠窗口和聊天窗口如何协同

## 20. 一句话总结

这个前端的价值不在“页面很多”，而在“Electron 主进程、桌宠交互、聊天状态、后端通信”四条链路已经串起来了。当前最重要的开发目标不是盲目加界面，而是先把配置化、稳定性和安全边界补齐，让它从一个能跑的桌宠原型逐步升级为可持续迭代的桌面应用。
