# 虚拟桌宠后端开发指南

> 本文档已根据当前源码（2026-07）核对更新。若与代码不一致，以代码为准。

## 1. 项目定位

`pet-api` 是虚拟桌宠项目的后端服务，承担四类职责：

1. 提供 `WebSocket` 实时消息通道（`/ws`），供桌宠客户端对话。
2. 提供 REST API：健康检查、客户端初始化配置、**API Token / 模型配置管理**。
3. 会话管理与持久化（基于 `session_id`，默认 SQLite）。
4. 大模型回复生成（OpenAI 协议，按用户配置；失败回退本地预设）。

**大模型已经接入**，不是"待开发"。是否走真实模型取决于该 `userid` 是否配置过 API Token。

## 2. 技术栈与运行方式

### 核心技术

- 运行时：Node.js（CommonJS）
- HTTP 框架：Express 5
- WebSocket：`ws`
- AI：OpenAI SDK（兼容任意 OpenAI 协议端点）
- 持久化：better-sqlite3 + bcryptjs
- 环境变量：dotenv

### 入口文件

- 服务入口：`server.js` → 调用 `src/app.js` 的 `createServer()`
- 主逻辑：`src/app.js`（建服务、挂路由/WS、端口探测）

### 启动方式

```bash
npm install
node server.js          # 生产/直接运行
npm run dev             # nodemon 开发（见下方注意事项）
```

> ⚠️ `package.json` 的 `dev` 脚本当前误写为两个相同的 `nodemon server.js`，
> 只会启动一个进程。建议改为 `"dev": "nodemon server.js"`。

## 3. 目录结构说明

```text
pet-api/
├─ server.js
├─ package.json
├─ .env                      # PORT / STORAGE_TYPE / MODEL_API_ENDPOINT
├─ PORTREADME.md
├─ 开发指南.md
├─ res/
│  └─ fallback_responses.json
└─ src/
   ├─ app.js                 # 建服务、挂路由/WS、端口探测
   ├─ api/
   │  ├─ routes.js           # REST 路由
   │  └─ requestSigner.js    # RSA 签名校验
   ├─ ai/
   │  └─ connectionAiContext.js  # 按 userid 建 OpenAI 上下文
   ├─ db/
   │  ├─ memoryStorage.js    # 内存存储
   │  └─ dbStorage.js        # SQLite 存储（当前激活）
   ├─ services/
   │  ├─ aiReplyService.js   # 大模型回复
   │  └─ fallbackService.js  # 关键词兜底回复
   ├─ session/
   │  └─ sessionManager.js   # 会话门面（STORAGE_TYPE 切换）
   ├─ token/
   │  └─ apiTokenManager.js  # Token/模型/端点读写
   ├─ utils/
   │  └─ portHelper.js       # 端口探测
   └─ websocket/
      └─ socketServer.js     # WS 消息处理核心
```

### 关键文件职责

- `src/app.js`：加载配置、建 Express/http 服务、挂 REST 与 WS、启动端口探测。
- `src/websocket/socketServer.js`：WS 连接与消息处理；校验 `content`/`userid`；调用大模型或兜底；推送欢迎语。
- `src/services/aiReplyService.js`：调用 `openai.chat.completions.create`，带系统提示与最近 6 条历史，`max_tokens: 512`，按内容动态超时。
- `src/services/fallbackService.js`：关键词命中 `fallback_responses.json` 取随机回复。
- `src/ai/connectionAiContext.js`：读 `apiTokenManager` 拿 Token/模型/端点，构建 OpenAI 客户端（timeout 60s，maxRetries 0）。
- `src/db/dbStorage.js`：建 `messages` / `api_tokens` / `api_clients` 三表，含 bcrypt 密码哈希。
- `src/session/sessionManager.js`：按 `STORAGE_TYPE` 动态加载 `memoryStorage` 或 `dbStorage`。
- `src/token/apiTokenManager.js`：Token 的加密存储与读写、查看密码校验。
- `src/api/requestSigner.js`：校验客户端请求签名（RSA verify + 时间戳 + nonce）。
- `res/fallback_responses.json`：桌宠"说什么/做什么/什么情绪"的兜底数据源。

## 4. 服务启动流程

1. `dotenv.config()` 加载 `.env`
2. 从 `res/fallback_responses.json` 读取预设回复
3. 初始化 Express 与 http.Server
4. 创建 `WebSocket.Server`，挂载在 `/ws`
5. 按端口候选列表探测可用端口
6. 监听 WS 消息与 REST 路由
7. 打印访问地址

### 端口策略

代码（`src/app.js`）先尝试 `process.env.PORT`（兜底默认 8089），再尝试候选端口：

```text
3000, 3001, 8089, 5000
```

`.env` 当前设置 `PORT=9089`，因此实际监听 **9089**。前端 `akisaki-kirari` 默认连 `ws://localhost:9089/ws`，三者一致。

> 联调风险：若 9089 被占用，后端会漂移到 3000 等端口，但前端不会自动发现。联调时务必保证 9089 空闲。

## 5. WebSocket 消息链路

### 客户端发送的消息

```json
{
  "type": "user_message",
  "session_id": "test-session",
  "userid": 1,
  "content": "你好"
}
```

- `content`：必填，非空字符串。
- `userid`：**不再由客户端决定**。连接身份在握手阶段由 `?token=` 会话令牌确定（见下方“连接鉴权”），服务端强制用令牌对应的 `uid`，忽略此处自报的 `userid`，防止冒用/切换用户。
- `session_id`：可选，缺省时服务端生成。
- 当前实现**不校验 `type` 字段**，直接取 `content`。

### 连接鉴权（握手阶段）

WebSocket 挂在 `/ws`，连接时**必须**在握手 query 携带会话令牌：`ws://host/ws?token=<token>`。

- 后端在 HTTP 升级前用 `verifyClient` 校验令牌：
  - 远程模式：无状态签名会话令牌（`verifySession`，HMAC-SHA256，7 天有效）；
  - 本地模式：内置账户明文令牌（值同 `BUILTIN_ACCOUNT_TOKEN`）。
- 校验失败 → 拒绝升级（HTTP 4401），连接根本不会建立，且不分配任何消息处理能力。
- 校验成功 → 该连接绑定到令牌对应的 `uid`，后���所有消息以该 `uid` 身份处理。

### 处理过程

1. 解析 JSON
2. 校验 `content` 非空 → 否则 `INVALID_MESSAGE`
3. 连接身份已在握手阶段确定（`authUid`），直接用于创建 `aiContext`；不信任客户端自报 `userid`
4. 首次为该连接创建 `aiContext`（按 `authUid`）
5. 初始化/写入会话，保存 user 消息
6. **优先调用大模型** `aiReplyService.getReply()`
7. 模型调用抛错 → **回退** `fallbackService.getReply()`（`source:"fallback"`）
8. 保存 assistant 回复
9. 返回 `pet_response`（含 `timestamp`）

### 服务端返回

```json
{
  "type": "pet_response",
  "speech": "主人好呀~",
  "action": "idle",
  "mood": "curious",
  "source": "model",
  "model": "GLM-4.7-Flash",
  "timestamp": 1700000000000
}
```

> 模型路径固定 `action:"idle"`、`mood:"curious"`；`source` 标明来自模型还是兜底。

### 连接欢迎消息

连接成功后约 **1.5 秒**主动推送一条欢迎 `pet_response`（演示性质，非用户触发）。

## 6. 回复生成机制

当前是**两级策略**：

- **一级（主）：大模型**。`aiReplyService` 用 OpenAI SDK 调用户配置端点，系统提示为"你是一个友善、简洁的虚拟桌宠助手"，带上最近 6 条历史，`max_tokens: 512`。
- **二级（兜底）：关键词预设**。仅当未配置 Token 或模型调用失败时触发，`fallbackService` 从 `fallback_responses.json` 随机取一条。

分类仍来自关键词命中：greeting / goodbye / praise / scold / status / time / weather / default。

> 历史文档把"先关键词、后大模型"描述为未来计划，与现状相反：现在是大模型优先、预设兜底。

## 7. 会话管理机制

`SessionManager` 统一对外暴露，底层通过 `STORAGE_TYPE` 选择 `memoryStorage.js` 或 `dbStorage.js`。

### 存储模式

```env
STORAGE_TYPE=db      # 当前 .env 激活
# STORAGE_TYPE=memory
```

- `memory`：仅当前进程，重启即丢失。
- `db`：SQLite 文件 `data/pet_sessions.db`，重启可恢复。

### 行为

- `initSession(sessionId)`：不存在则创建
- `saveMessage(sessionId, role, content)`：追加消息
- `getRecentMessages(sessionId, limit)`：读取最近若干条（模型调用取 6 条）

### 限制

- 仅保留最近 10 条消息（约 5 轮）
- 模型上下文取最近 6 条

## 8. API Token 与签名体系

除 `/health`、`/config` 外，所有 `/api-client`、`/api-token` 接口都需客户端签名。

### 客户端身份（前端生成）

- 前端在 `userData/client-identity.json` 生成 **ed25519** 密钥对，`clientId` 为 UUID。
- 请求头：`X-Client-Id`、`X-Client-Timestamp`、`X-Client-Nonce`、`X-Client-Signature`。
- 签名原文：`method\npath\ntimestamp\nnonce\nJSON(body)`，用 ed25519 私钥签名。

### 后端校验（`requestSigner.js`）

- 用客户端公钥做 **RSA `crypto.verify`**，校验签名 + 时间戳新鲜度 + nonce 未重放。
- 客户端需在 `/api-client/register` 先注册 `clientId` + `publicKey`，并绑定 `userid`（当前前端固定 `userid=1`）。

### Token 存储

- `apiTokenManager` 把 Token 加密存于 SQLite `api_tokens` 表；查看密码用 bcrypt 哈希。
- 模型名默认 `GLM-4.7-Flash`，端点默认 `https://open.bigmodel.cn/api/paas/v4/`。

## 9. REST 接口说明

### 公共接口

- `GET /health` → `{status, message, version, uptime}`（无 model 字段）
- `GET /config` → `{default_action, max_message_length, supports_tts, mode:"fallback", version}`
- `GET /` → 文本 "Virtual Pet API is running!"

### 需签名的接口（Token 管理）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api-client/register` | 注册客户端身份（clientId + publicKey 绑定 userid） |
| GET  | `/api-token/status` | 查询是否已配置 Token 及模型配置 |
| POST | `/api-token` | 设置 Token + 查看密码 + 模型 + 端点 |
| POST | `/api-token/model` | 单独更新模型名 |
| POST | `/api-token/endpoint` | 单独更新端点 |
| POST | `/api-token/verify` | 用查看密码校验并取回 Token |

> 完整字段与示例见 `PORTREADME.md`。

## 10. 环境变量

```env
PORT=9089
STORAGE_TYPE=db
MODEL_API_ENDPOINT=https://open.bigmodel.cn/api/paas/v4/
```

API Token、模型名、端点由前端"设置"页保存到后端数据库；前端保存的端点覆盖 `MODEL_API_ENDPOINT` 默认值。

## 11. 开发注意事项

1. **前后端端口一致性**：后端自动选端口，前端写死 9089。联调取确保 9089 空闲。
2. **`WS` 消息需 `userid`**：缺 `userid` 会直接返回错误，旧文档示例不含该字段。
3. **模型失败会静默兜底**：未配 Token 时回复来自 `fallback_responses.json`，不是模型。
4. **签名两端一致**：改签名算法需前后端同步。
5. **`getRandomReply()` 依赖配置结构**：改 `fallback_responses.json` 保持结构兼容。
6. **尚无速率限制 / 消息长度限制 / 细粒度错误码**。

## 12. 推荐开发流程

```bash
npm install
# 配置 .env（PORT=9089）
node server.js

curl http://localhost:9089/health
curl http://localhost:9089/config

wscat -c ws://localhost:9089/ws
{"type":"user_message","session_id":"test","userid":1,"content":"你好"}
```

联调建议：先起后端，再起前端；优先验证 `/health` 再验证 `/ws`。

## 13. 后续重构建议

### 近期

- 修复 `dev` 脚本（去掉重复的 nodemon）
- 端口协商：让前端能感知后端实际端口（如启动探测 `/health`）
- 增加消息长度限制、速率限制
- 补齐最小单元测试

### 中期

- **接入 OneBot / QQ 适配器**（当前最大缺口）
- 多 `userid` 映射（支持多 QQ 号 / 群）
- 日志分层、配置校验

### 长期

- 工具调用、用户画像与长期记忆
- 多模态（图片/语音）
- 更丰富的桌宠动作与情绪协议

## 14. 新人接手阅读顺序

1. `server.js`
2. `src/app.js`
3. `src/websocket/socketServer.js`
4. `src/services/aiReplyService.js` + `src/services/fallbackService.js`
5. `src/ai/connectionAiContext.js`
6. `src/session/sessionManager.js` + `src/db/dbStorage.js`
7. `src/api/routes.js` + `src/api/requestSigner.js`
8. `PORTREADME.md`

## 15. 一句话总结

这个后端已不是"纯 MVP 骨架"：大模型回复、SQLite 持久化、Token 签名体系均已落地。当前重点是**补齐 OneBot/QQ 适配层**、加固安全边界（端口协商、限流）与可观测性，而不是重做通信骨架。
